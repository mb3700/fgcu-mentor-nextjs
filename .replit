run = "npm run start"
entrypoint = "src/app/page.tsx"

[nix]
channel = "stable-24_05"

[env]
PORT = "3000"

[deployment]
run = ["sh", "-c", "npx prisma db push && npm run build && npm run start"]
deploymentTarget = "autoscale"

[[ports]]
localPort = 3000
externalPort = 80
